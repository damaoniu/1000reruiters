app.controller('ResetPassCtrl',resetPassController);
    resetPassController.$inject=['$scope','$meteor','$alert'];
    function resetPassController($scope,$meteor,$alert){
        var vm =$scope;

    }